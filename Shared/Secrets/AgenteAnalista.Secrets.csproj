<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>AgenteAnalista.Secrets</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Configuration.Abstractions" Version="10.0.0" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Json" Version="10.0.0" />
  </ItemGroup>

  <!-- Cifrado de secretos propio de este repo (AES-256-GCM) — copia independiente del que usa
       PanelMasterHelp (no hay referencia cruzada entre repos). Cada MCP que necesite credenciales
       (DragonfishApiMcp, ZlApiMcp, y a futuro el de SQL) referencia este proyecto. -->
</Project>
